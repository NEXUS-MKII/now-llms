{
  "name": "now-llms",
  "version": "1.0.0",
  "description": "NOW Group llms.txt worker — serves AI context files at nowgroup.co.nz",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
